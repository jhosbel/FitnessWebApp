import React from 'react'
import { montserrat } from '../ui/fonts'

export default function Training() {
  return (
    <div className={`${montserrat.className} antialiased min-h-screen w-4/5 right-0 absolute`}>Training</div>
  )
}
